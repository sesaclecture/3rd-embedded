sudo apt-get install build-essential gcc-aarch64-linux-gnu \
    bc git wget libssl-dev make libc6-dev libncurses5-dev \
    bison flex libgnutls28-dev

git clone https://github.com/u-boot/u-boot.git
cd u-boot
make distclean
make rpi_arm64_defconfig
make -j$(nproc) CROSS_COMPILE=aarch64-linux-gnu-

-----

cd configs
ll * | grep rpi

-----------

help
version
bdinfo

mmc list
mmc dev 0
fatls mmc 0:1

boot
run bootcmd

printenv
printenv bootcmd
printenv bootargs -> cat /proc/cmdline

printenv bootdelay
setenv bootdelay 5
saveenv
